# MediLink Patient Mobile App

React Native + Expo mobile application — built in Week 6C.

## Stack
- React Native with Expo
- Firebase Cloud Messaging for push notifications
- Uses `@medilink/shared` for types

## Imports
- Types from `@medilink/shared`
